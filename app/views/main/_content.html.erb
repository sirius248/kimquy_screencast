
<div class="container">
    <!-- Begin Flash message -->
    <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, id: "flash_#{name}" %>
    <% end %>
    <!-- End Flash message -->
    <%= yield %>

  </div>